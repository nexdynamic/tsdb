A minimal time series database in Rust.

**What it does:**
- Insert points (timestamp, value) into a buffer
- Flush buffer to disk as binary chunks
- Query time ranges and get matching points back

We will do compressio and indexing next
